[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"

[functions."*"]
  timeout = 900

[[plugins]]
  package = "@netlify/plugin-nextjs"
